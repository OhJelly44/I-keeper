// 실습 2
// 백준 – 10871번: X보다 작은 수
// 수열의 길이와 정수 X 입력받고
// 수열에서 X보다 작은 수를 출력한다.

/*
#include <stdio.h>

int main() 
{
    int N, X;

    scanf_s("%d %d", &N, &X);

    int A[10000];

    for (int i = 0; i < N; i++)
    {
        scanf_s("%d", &A[i]);
    }

    for (int i = 0; i < N; i++) {
        if (A[i] < X) {
            printf("%d ", A[i]);
        }
    }

    return 0;
}
*/
