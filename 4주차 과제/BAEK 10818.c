// น้มุ 10818น๘ 
/*
#include <stdio.h>

int main(void)
{
    int N;
    scanf_s("%d", &N);

    int num;
    int min, max;

    scanf_s("%d", &num);
    min = max = num;

    for (int i = 1; i < N; i++) 
    {
        scanf_s("%d", &num);
        if (num < min) 
        {
            min = num;
        }
        if (num > max) 
        {
            max = num;
        }
    }

    printf("%d %d\n", min, max);

    return 0;
}
*/